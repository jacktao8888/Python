##递归调用与汉诺塔算法
  def move(n,a,b,c):
    if n==1:
      print a,"-->",c
      return          #此处如果没有return，会导致错误RuntimeError: maximum recursion depth exceeded
    move(n-1,a,c,b)
    move(1,a,b,c)
    move(n-1,b,a,c)
